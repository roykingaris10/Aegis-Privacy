import * as React from "react";
import { PlaceholderPage } from "@/components/placeholder-page";

export default function LearnPage(): React.ReactElement {
  return (
    <PlaceholderPage
      title="Learn"
      sprint="Sprint 3"
      description="Learning resources hub with guides on DPIAs, SARs, FOI, transfers, and more. Coming in Sprint 3."
    />
  );
}
