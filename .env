DATABASE_URL="file:./dev.db"

# NextAuth base URL for the local dev server.
AUTH_URL="http://localhost:3000"

# AUTH_SECRET is required by NextAuth. This is a dev-only value; production
# should set its own value in .env.local or Vercel env vars.
AUTH_SECRET="aegis-local-dev-only-not-for-production"
